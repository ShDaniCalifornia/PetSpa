<Window x:Class="PetSpa.Views.Windows.WarningForgetPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PetSpa.Views.Windows"
        mc:Ignorable="d"
        Title="WarningForgetPasswordWindow" Height="150" Width="350" 
        WindowStyle="None"
        AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid>
        <Border CornerRadius="30" Background="White"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="270"/>
                <ColumnDefinition Width="40"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="70"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Column="1" Text="Собщение" FontSize="10" Foreground="black"
                       HorizontalAlignment="Left"
                        Style="{StaticResource HeaderTextBlockStyle}"/>

            <TextBlock Grid.Row="1" Grid.Column="1" TextWrapping="Wrap" HorizontalAlignment="Center" Text="Если вы забыли пароль, оставьте заявку в службе поддержки!"
                        Style="{StaticResource OrangeTextBlockStyle}"/>

            <Button Grid.Column="1" Grid.Row="2" 
                    Name="OKBtn" Click="OKBtn_Click" Content="OK" Width="120" Height="30"
                    Margin="0 5 0 5"
                    Style="{StaticResource OrangeWhiteButtonStyle}"/>

            <Image Source="/Resources/Icons/DogMessage.png" 
                   Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="2"
                   HorizontalAlignment="Right" Margin="0 -30 10 -10"/>


        </Grid>
    </Grid>
</Window>

