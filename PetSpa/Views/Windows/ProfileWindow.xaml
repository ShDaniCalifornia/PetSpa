<Window x:Class="PetSpa.Views.Windows.ProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PetSpa.Views.Windows"
        xmlns:appdata="clr-namespace:PetSpa.AppData"
        mc:Ignorable="d"
        Height="400" Width="280"
        WindowStyle="None"
        AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <appdata:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="300"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>

        <!-- Фото для админа (видно когда IsAdmin = true) -->
        <Border Grid.Row="0" Grid.RowSpan="2"
                CornerRadius="30,30,0,0"
                ClipToBounds="True"
                Visibility="{Binding IsAdmin, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Image Source="/Resources/Images/adminPhoto.png" 
                   Stretch="UniformToFill"/>
        </Border>

        <!-- Оранжевый фон с лапкой для клиента (видно когда IsAdmin = false) -->
        <Border Grid.Row="0" Grid.RowSpan="2"
                CornerRadius="30,30,0,0"
                Background="#DF6C2D"
                Visibility="{Binding IsAdmin, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=invert}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="🐾" FontSize="80" Foreground="White"/>
                <TextBlock Text="PetSpa" FontSize="20" Foreground="White" 
                           HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="25"/>
                <RowDefinition Height="25"/>
                <RowDefinition Height="25"/>
                <RowDefinition Height="25"/>
            </Grid.RowDefinitions>

            <Border Background="#DF6C2D" Grid.RowSpan="4"
                    VerticalAlignment="Bottom"
                    CornerRadius="40"
                    Margin="0 0 0 0">

                <StackPanel Margin="15,10"
                        VerticalAlignment="Center">

                    <TextBlock Text="{Binding FullName}"
                           FontWeight="Bold" 
                           FontSize="16" 
                           Foreground="White" 
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"
                           Margin="0,0,0,5"/>

                    <TextBlock Text="{Binding RoleName}"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="#FFF0E6"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,8"/>

                    <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Margin="0,0,0,5">
                        <TextBlock Text="{Binding Phone}"
                               FontSize="10"
                               Foreground="White"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center">
                        <TextBlock Text="{Binding Email}"
                               FontSize="10"
                               Foreground="White"
                               TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>